read("Matrix-morphology-10Feb2016-123-taxa-nodash.ss")
--read("heteroptera/*.fasta")
read("heteroptera-dW-1-poy.tre")
build(dwag)
-- select(best)
-- swap(spr, steepest)
-- select(best)
-- support(jackknife, buildOnly, replicates:10)
report("heteroptera-pg.tre", graphs,newick, overwrite)
-- report("het-pair-dist.csv", pairdist, overwrite)
report("het-pg-data.csv", data, overwrite)
report("het-pg-diag.csv", diagnosis, overwrite)
