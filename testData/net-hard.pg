read("net-I.fas")
read("net-II.fas")
--read("net-Ia.fas")
--read("net-IIa.fas")
--reblock ("net1", "net-I*.fas")
--reblock ("net2", "net-II*.fas")
--read(block:"netBlock.txt")
--read("net-II.fas")
--read("net-I.dot")
--read("net-I.tre")
--read("net-II.tre")
--read("net-II.dot")
--read("net-I-II.dot")
read("net-I-II.dot")
--read("net-I-II-extra.dot")
-- set(seed:0)
set(outgroup:"zero")
--set(graphtype:hardwired)
set(graphtype:softwired)
-- set(graphFactor:NoPenalty)
--set(graphFactor:W15)
--set(rootcost:NoRootCost)
--set(rootcost:W15)
set(finalassignment:DO)
--build(distance, rdwag, replicates:10)
--select(unique)
-- build(block, distance, rdwag, nj, graph, displayTrees:5, replicates:10)
-- swap(spr, steepest, keep:5)
-- select(unique)
-- swap(tbr, all, keep:5)
-- select(unique)
-- fuse()
-- swap(spr, steepest)
swap(tbr, drift, maxChanges:5, acceptequal:0.0)
select(unique)
refine(netadd)
select(unique)
refine(netadd, drift, maxChanges:5, acceptequal:0.0)
select(unique)
--refine(netdel)
select(unique)
--refine(netdel, drift, maxChanges:5, acceptequal:0.0)
select(unique)
refine(netmove)
select(unique)
refine(netmove, drift, maxChanges:5, acceptequal:0.0)
select(unique)
swap(all, tbr)
select(best)
report("net-hard-test.tre", graphs, newick, overwrite)
report("net-hard-test.dot", graphs, dot, overwrite)
report("net-hard-test-data.csv", data, overwrite)
report("net-hard-test-diag-IA.csv", diagnosis, overwrite)
report("net-display.dot", displaytrees, dot, overwrite)
report("net-display.tre", displaytrees, newick, overwrite)
report("net-test-reconcile.dot", reconcile, dot, overwrite, method:cun, threshold:51)
